export enum StreamStatus {
  Waiting = "waiting",
  Live = "live",
  Ended = "ended",
}

export enum DisplayMessageType {
  User = "user",
  System = "system",
}

export enum ChatMessageType {
  Text = "text",
  Emoji = "emoji",
  Gift = "gift",
}
